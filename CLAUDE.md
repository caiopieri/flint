# Flint

**STOP. Before reading code, answering, or making any change in this repo, open and read [`AGENTS.md`](./AGENTS.md) in full.** It is the canonical guide and contains **binding, non-negotiable invariants** (local-first, files-as-truth, the JS/native/bridge boundary, naming rules, and a do-not-reintroduce list). Do not write code, scaffold, or propose architecture until you have read it — several "obvious" approaches are explicitly refuted there.

After `AGENTS.md`, read [`docs/ARCHITECTURE.md`](./docs/ARCHITECTURE.md) (how it's built) and [`docs/DECISIONS.md`](./docs/DECISIONS.md) (why, with rejected alternatives).

This file intentionally holds no rules of its own — `AGENTS.md` is the single source so nothing drifts out of sync.
