// AI — on-device inference + explicit routing (Phase 3, not Phase 1).
//
// Local = light models only (llama.cpp/Metal, evaluate MLX). Heavy work routes
// explicitly via Flows to cloud or the user's VPS — no automatic heuristic.
// Stub only; do NOT start this in Phase 1 (see ROADMAP.md / AGENTS.md).
import Foundation

enum AI {
    // Local inference + routing land in Phase 3.
}
