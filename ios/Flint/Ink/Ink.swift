// Ink — Apple Pencil handwriting (Phase 1b).
//
// A native PKCanvasView page with paper templates. Saved as its own vault file
// (PKDrawing + rendered PNG/SVG) and embedded in notes via ![[sketch.ink]].
// Scope is LOCKED: one page, save, embed, open — no infinite canvas/brushes/layers.
// Implemented in T6/T7 (see docs/TASKS.md). Stub for now.
import Foundation

enum Ink {
    // PencilKit canvas lands in Phase 1b.
}
