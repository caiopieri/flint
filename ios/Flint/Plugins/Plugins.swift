// Plugins — the plugin host (Phase 2, not Phase 1).
//
// Loads web plugins (isolated WKWebView), enforces the capability manifest via
// the Bridge. The public Plugin API is EXTRACTED later, with Ink/Board/Flows as
// its first consumers — do NOT design it in a vacuum during Phase 1.
// Stub only (see AGENTS.md / docs/TASKS.md).
import Foundation

enum Plugins {
    // Plugin host + capability enforcement land in Phase 2.
}
